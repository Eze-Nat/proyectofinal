    <div class="success-container">
        <h2 class="success-heading">Tu compra se realizó con éxito</h2>

        <div class="purchased-products">
            <h4>Productos Comprados</h4>
            <!-- Add the list of purchased products here -->
        </div>
    </div>
{{!-- <ul>
    {{#each purchasedProducts}}
        <li>{{this.product.title}} - {{this.quantity}}</li>
    {{/each}}
</ul>

<h4>Productos No Comprados</h4>
<ul>
    {{#each notPurchasedProducts}}
        <li>{{this.product.title}} - {{this.quantity}}</li>
    {{/each}}
</ul> --}}